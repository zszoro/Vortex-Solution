import type {MetadataRoute} from "next";import {demos,projects,siteConfig} from "@/data/site";export default function sitemap():MetadataRoute.Sitemap{return ["","/privacidade","/termos",...projects.map(p=>`/projetos/${p.slug}`),...demos.map(d=>`/demonstracoes/${d.slug}`)].map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly":"monthly",priority:path===""?1:.7}))}
