import {NextResponse} from "next/server";import {cookies} from "next/headers";import {createSessionToken} from "@/lib/auth";
export async function GET(req:Request){const url=new URL(req.url);const state=url.searchParams.get("state");const code=url.searchParams.get("code");if(!code||state!==(await cookies()).get("google_oauth_state")?.value)return NextResponse.redirect(new URL("/login?google=invalid",req.url));const token=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:process.env.GOOGLE_CLIENT_ID!,client_secret:process.env.GOOGLE_CLIENT_SECRET!,redirect_uri:new URL("/api/auth/google/callback",req.url).toString(),grant_type:"authorization_code"})});const data=await token.json();const profile=await fetch(`https://oauth2.googleapis.com/tokeninfo?id_token=${data.id_token}`).then(r=>r.json());if(profile.email!=="zszoro818@gmail.com")return NextResponse.redirect(new URL("/login?google=denied",req.url));const res=NextResponse.redirect(new URL("/admin",req.url));res.cookies.set("vortex_session",createSessionToken(),{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:60*60*24*7});res.cookies.delete("google_oauth_state");return res}
