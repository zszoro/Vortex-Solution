import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {ArrowLeft,ArrowRight,CheckCircle2} from "lucide-react";import {Footer} from "@/components/Footer";import {Header} from "@/components/Header";import {projects} from "@/data/site";
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=projects.find(x=>x.slug===slug);return p?{title:p.name,description:p.description}:{}}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <><Header/><main className="case"><div className="container"><Link className="back" href="/#projetos"><ArrowLeft/> Voltar aos projetos</Link><span className="eyebrow">Projeto conceitual · {p.category}</span><h1>{p.name}</h1><p className="case-lead">{p.description} Este case é uma demonstração de capacidade e não representa um cliente real.</p><div className={`case-visual ${p.tone}`}><div className="case-browser"><span>VORTEX CONCEPT / {p.year}</span><h2>{p.name}</h2><p>Estratégia, tecnologia e experiência conectadas.</p><i/><i/><i/></div></div><div className="case-grid"><section><span className="eyebrow">O desafio</span><h2>Transformar descoberta em uma jornada simples.</h2><p>O conceito parte de uma operação que precisa apresentar conteúdo, orientar escolhas e converter interesse sem adicionar complexidade. A arquitetura equilibra narrativa, catálogo e ações claras.</p></section><section><span className="eyebrow">A solução</span><h2>Uma experiência preparada para crescer.</h2><ul><li><CheckCircle2/> Interface responsiva e autoral</li><li><CheckCircle2/> Fluxos objetivos de navegação</li><li><CheckCircle2/> Componentes escaláveis</li><li><CheckCircle2/> Base pronta para integrações</li></ul></section></div><div className="case-meta"><div><span>Categoria</span><b>{p.category}</b></div><div><span>Ano</span><b>{p.year}</b></div><div><span>Tecnologias</span><b>{p.tech.join(" · ")}</b></div></div><Link className="btn primary" href={`/demonstracoes/${p.slug.includes("nexus")?"loja-gamer":p.slug.includes("aura")?"moda":p.slug.includes("volt")?"eletronicos":p.slug.includes("saborize")?"restaurante":"marketplace"}`}>Abrir experiência navegável <ArrowRight/></Link></div></main><Footer/></>}
